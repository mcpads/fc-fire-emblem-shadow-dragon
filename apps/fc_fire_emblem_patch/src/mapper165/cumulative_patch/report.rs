use serde::Serialize;

mod assembly;

pub(super) use assembly::{CumulativeReportInputs, assemble_cumulative_report};

#[derive(Debug, Serialize)]
pub(super) struct CumulativePatchReport {
    pub(super) schema: u8,
    pub(super) source_sha1: &'static str,
    pub(super) output_sha1: String,
    pub(super) output_mapper: u16,
    pub(super) prg_size: usize,
    pub(super) chr_size: usize,
    pub(super) stage_count: usize,
    pub(super) stages: Vec<CumulativeStageReport>,
    pub(super) chapter_titles: CumulativeChapterTitleReport,
    pub(super) main_dialogue: CumulativeDialogueReport,
    pub(super) options_menu: CumulativeOptionsMenuReport,
    pub(super) front_end_menu: CumulativeFrontEndMenuReport,
    pub(super) playable_unit_names: CumulativeUnitNameReport,
    pub(super) automatic_class_profiles: CumulativeClassProfileReport,
    pub(super) title_logo: CumulativeTitleLogoReport,
    pub(super) weapon_shop_shared_text: CumulativeWeaponShopSharedTextReport,
    pub(super) battle_text: CumulativeBattleTextReport,
    pub(super) selector_fallback_graph: SelectorFallbackGraphReport,
    pub(super) original_chr_preserved: bool,
    pub(super) tracked_write_count: usize,
    pub(super) translation_input_complete: bool,
    pub(super) review_complete: bool,
    pub(super) runtime_verified: bool,
    pub(super) unresolved: Vec<&'static str>,
    pub(super) release_eligible: bool,
}

#[derive(Debug, Serialize)]
pub(super) struct CumulativeTitleLogoReport {
    pub(super) workspace_sha1: String,
    pub(super) asset_sha1: String,
    pub(super) source_owned_tile_count: usize,
    pub(super) installed_unique_tile_count: usize,
    pub(super) installed_tilemap_cell_count: usize,
    pub(super) physical_chr_page: u8,
    pub(super) installed_chr_page_sha1: String,
    pub(super) installed_stream_sha1: String,
    pub(super) installed_runtime_cleared_top_strip_cell_count: usize,
    pub(super) installed_runtime_reasserted_logo_cell_count: usize,
    pub(super) installed_runtime_completion_stream_sha1: String,
    pub(super) preserved_title_stream_bytes_unchanged: bool,
    pub(super) preserved_runtime_completion_control_bytes_unchanged: bool,
    pub(super) unassigned_title_chr_patterns_unchanged: bool,
    pub(super) source_sword_sprite_tm_and_copyright_assets_unchanged: bool,
    pub(super) runtime_evidence_manifest_sha1: String,
    pub(super) runtime_sample_count: usize,
    pub(super) runtime_unique_image_count: usize,
    pub(super) runtime_bound_to_build: bool,
    pub(super) review_complete: bool,
}

#[derive(Debug, Serialize)]
pub(super) struct CumulativeOptionsMenuReport {
    pub(super) installed_entry_count: usize,
    pub(super) screen_evidence_manifest_sha1: String,
    pub(super) temporal_sample_count: usize,
    pub(super) unique_nametable_count: usize,
    pub(super) observed_row_states: Vec<u8>,
    pub(super) target_glyph_count: usize,
    pub(super) visible_active_code_count: usize,
    pub(super) preserved_active_code_count: usize,
    pub(super) total_slot_demand: usize,
    pub(super) capacity_bound_to_build: bool,
}

#[derive(Debug, Serialize)]
pub(super) struct CumulativeBattleTextReport {
    pub(super) fixed_text_workspace_sha1: String,
    pub(super) dialogue_workspace_sha1: String,
    pub(super) temporal_manifest_sha1: String,
    pub(super) runtime_base_report_sha1: String,
    pub(super) loader_report_sha1: String,
    pub(super) installed_fixed_entry_count: usize,
    pub(super) installed_unit_name_count: usize,
    pub(super) installed_enemy_name_count: usize,
    pub(super) installed_class_name_count: usize,
    pub(super) installed_item_name_count: usize,
    pub(super) installed_terrain_name_count: usize,
    pub(super) installed_battle_message_template_count: usize,
    pub(super) installed_battle_forecast_label_count: usize,
    pub(super) weapon_shop_item_names_subset_of_battle_catalog: bool,
    pub(super) installed_dialogue_record_count: usize,
    pub(super) installed_translated_line_count: usize,
    pub(super) stable_color_count: usize,
    pub(super) glyph_atlas_tile_count: usize,
    pub(super) observed_runtime_tuple_count: usize,
    pub(super) maximum_observed_overlay_count: usize,
    pub(super) maximum_observed_ppu_write_count: usize,
    pub(super) runtime_routine_byte_count: usize,
    pub(super) text_diff_range_count: usize,
    pub(super) cumulative_selector_ranges_preserved: bool,
    pub(super) original_english_digits_and_graphics_preserved: bool,
    pub(super) runtime_bound_to_build: bool,
    pub(super) review_complete: bool,
}

#[derive(Debug, Serialize)]
pub(super) struct CumulativeWeaponShopSharedTextReport {
    pub(super) screen_role: &'static str,
    pub(super) fixed_text_workspace_sha1: String,
    pub(super) choice_label_workspace_sha1: String,
    pub(super) installed_item_name_count: usize,
    pub(super) installed_choice_label_count: usize,
    pub(super) projected_item_pointer_count: usize,
    pub(super) item_string_byte_count: usize,
    pub(super) choice_string_byte_count: usize,
    pub(super) shared_page_unique_glyph_count: usize,
    pub(super) shared_page_preserved_active_code_count: usize,
    pub(super) shared_page_total_slot_demand: usize,
    pub(super) added_glyph_count: usize,
    pub(super) glyph_assignment_sha1: String,
    pub(super) font_physical_page: u8,
    pub(super) font_mapper_register: u8,
    pub(super) font_page_sha1: String,
    pub(super) font_page_pack_sha1: String,
    pub(super) item_list_pointer_selector_installed: bool,
    pub(super) selected_item_pointer_selector_installed: bool,
    pub(super) choice_pointer_selector_installed: bool,
    pub(super) unconverted_consumers_fallback_to_source_tables: bool,
    pub(super) capacity_bound_screen_roles: Vec<&'static str>,
    pub(super) runtime_evidence_manifest_sha1: String,
    pub(super) runtime_evidence_output_sha1: String,
    pub(super) runtime_sample_count: usize,
    pub(super) runtime_unique_image_count: usize,
    pub(super) runtime_bound_dialogue_screen_roles: Vec<&'static str>,
    pub(super) runtime_bound_item_name_screen_roles: Vec<&'static str>,
    pub(super) runtime_bound_choice_label_screen_roles: Vec<&'static str>,
    pub(super) runtime_bound_to_stage_output: bool,
    pub(super) runtime_carried_forward_by_verified_writes: bool,
    pub(super) review_complete: bool,
}

#[derive(Debug, Serialize)]
pub(super) struct CumulativeClassProfileReport {
    pub(super) workspace_sha1: String,
    pub(super) workspace_entry_count: usize,
    pub(super) installed_entry_count: usize,
    pub(super) installed_description_line_count: usize,
    pub(super) installed_source_storage_byte_count: usize,
    pub(super) installed_output_storage_byte_count: usize,
    pub(super) total_unique_glyph_count: usize,
    pub(super) page_unique_glyph_counts: [usize; 2],
    pub(super) glyph_assignment_sha1s: [String; 2],
    pub(super) font_physical_pages: [u8; 2],
    pub(super) font_mapper_registers: [u8; 2],
    pub(super) font_page_sha1s: [String; 2],
    pub(super) font_page_pack_sha1: String,
    pub(super) screen_evidence_manifest_sha1: String,
    pub(super) temporal_sample_count: usize,
    pub(super) unique_image_count: usize,
    pub(super) runtime_evidence_manifest_sha1: String,
    pub(super) runtime_sample_count: usize,
    pub(super) runtime_unique_image_count: usize,
    pub(super) visible_code_count: usize,
    pub(super) preserved_active_code_count: usize,
    pub(super) original_english_digits_and_ui_preserved: bool,
    pub(super) profile_index_page_selector_installed: bool,
    pub(super) runtime_bound_to_build: bool,
    pub(super) review_complete: bool,
}

#[derive(Debug, Serialize)]
pub(super) struct CumulativeUnitNameReport {
    pub(super) workspace_sha1: String,
    pub(super) workspace_entry_count: usize,
    pub(super) unique_glyph_count: usize,
    pub(super) roster_page_target_glyph_count: usize,
    pub(super) roster_page_preserved_active_code_count: usize,
    pub(super) roster_page_total_slot_demand: usize,
    pub(super) roster_projection_byte_count: usize,
    pub(super) unit_ui_projection_byte_count: usize,
    pub(super) roster_assignment_sha1: String,
    pub(super) unit_ui_assignment_sha1: String,
    pub(super) roster_page_pack_sha1: String,
    pub(super) unit_ui_page_pack_sha1: String,
    pub(super) unit_ui_font_physical_page: u8,
    pub(super) unit_ui_font_mapper_register: u8,
    pub(super) screen_evidence_manifest_sha1: String,
    pub(super) temporal_sample_count: usize,
    pub(super) unique_nametable_count: usize,
    pub(super) preserved_unit_ui_code_count: usize,
    pub(super) roster_projection_installed: bool,
    pub(super) unit_summary_projection_installed: bool,
    pub(super) source_battle_table_preserved: bool,
    pub(super) source_ending_table_preserved: bool,
    pub(super) roster_capacity_bound_to_build: bool,
    pub(super) runtime_bound_to_build: bool,
    pub(super) review_complete: bool,
}

#[derive(Debug, Serialize)]
pub(super) struct CumulativeFrontEndMenuReport {
    pub(super) workspace_sha1: String,
    pub(super) workspace_entry_count: usize,
    pub(super) installed_entry_count: usize,
    pub(super) installed_source_storage_byte_count: usize,
    pub(super) installed_output_storage_byte_count: usize,
    pub(super) original_english_and_digits_preserved: bool,
    pub(super) screen_evidence_manifest_sha1: String,
    pub(super) temporal_sample_count: usize,
    pub(super) unique_nametable_count: usize,
    pub(super) unique_glyph_count: usize,
    pub(super) glyph_assignment_sha1: String,
    pub(super) preserved_screen_active_code_count: usize,
    pub(super) preserved_source_active_code_count: usize,
    pub(super) preserved_result_dialogue_active_code_count: usize,
    pub(super) preserved_active_code_count: usize,
    pub(super) font_physical_page: u8,
    pub(super) font_mapper_register: u8,
    pub(super) font_page_sha1: String,
    pub(super) font_page_pack_sha1: String,
    pub(super) central_fe_companion_refresh_routed: bool,
    pub(super) no_save_source_lifetime_bound: bool,
    pub(super) save_slot_selection_source_lifetime_bound: bool,
    pub(super) runtime_variants_bound_to_build: bool,
    pub(super) review_complete: bool,
}

#[derive(Debug, Serialize)]
pub(super) struct CumulativeChapterTitleReport {
    pub(super) workspace_sha1: String,
    pub(super) workspace_entry_count: usize,
    pub(super) translated_entry_count: usize,
    pub(super) installed_entry_count: usize,
    pub(super) installed_chapter_indices: Vec<u8>,
    pub(super) installed_source_storage_byte_count: usize,
    pub(super) installed_output_storage_byte_count: usize,
    pub(super) original_digits_preserved: bool,
    pub(super) intro_title_table_installed: bool,
    pub(super) ending_scroll_duplicate_installed: bool,
    pub(super) review_complete: bool,
}

#[derive(Debug, Serialize)]
pub(super) struct CumulativeStageReport {
    pub(super) role: &'static str,
    pub(super) output_sha1: String,
    pub(super) report_sha1: Option<String>,
}

#[derive(Debug, Serialize)]
pub(super) struct CumulativeDialogueReport {
    pub(super) workspace_sha1: String,
    pub(super) workspace_record_count: usize,
    pub(super) workspace_filled_line_count: usize,
    pub(super) installed_record_count: usize,
    pub(super) installed_translated_line_count: usize,
    pub(super) installed_shared_page_glyph_slot_count: usize,
    pub(super) source_storage_byte_count: usize,
    pub(super) planned_storage_byte_count: usize,
    pub(super) remaining_storage_byte_count: usize,
    pub(super) lifetimes: Vec<CumulativeDialogueLifetimeReport>,
    pub(super) maximum_page_reloaded_lifetime: CumulativeMaximumDialogueReport,
}

#[derive(Debug, Serialize)]
pub(super) struct CumulativeMaximumDialogueReport {
    pub(super) screen_role: &'static str,
    pub(super) target_record_id: String,
    pub(super) workspace_sha1: String,
    pub(super) record_page_boundary_topology_sha1: String,
    pub(super) screen_evidence_manifest_sha1: String,
    pub(super) page_boundary_manifest_sha1: String,
    pub(super) page_boundary_observation_output_sha1: String,
    pub(super) installed_translated_line_count: usize,
    pub(super) source_storage_byte_count: usize,
    pub(super) planned_storage_byte_count: usize,
    pub(super) remaining_storage_byte_count: usize,
    pub(super) completed_page_count: usize,
    pub(super) display_lines_per_page: usize,
    pub(super) font_group_count: usize,
    pub(super) page_group_indices: Vec<usize>,
    pub(super) group_page_counts: Vec<usize>,
    pub(super) group_unique_glyph_counts: Vec<usize>,
    pub(super) glyph_assignment_sha1s: Vec<String>,
    pub(super) preserved_screen_active_code_count: usize,
    pub(super) preserved_source_active_code_count: usize,
    pub(super) preserved_active_code_count: usize,
    pub(super) temporal_sample_count: usize,
    pub(super) unique_nametable_count: usize,
    pub(super) font_physical_pages: Vec<u8>,
    pub(super) font_mapper_registers: Vec<u8>,
    pub(super) font_page_sha1s: Vec<String>,
    pub(super) font_page_pack_sha1: String,
    pub(super) completed_page_pointers_hex: Vec<String>,
    pub(super) group_transition_pointers_hex: Vec<String>,
    pub(super) initial_selector_byte_count: usize,
    pub(super) font_group_selector_byte_count: usize,
    pub(super) completed_page_transition_byte_count: usize,
    pub(super) completed_page_reload_installed: bool,
    pub(super) final_page_exit_bypasses_reload: bool,
    pub(super) original_english_and_digits_preserved: bool,
    pub(super) runtime_evidence_manifest_sha1: Option<String>,
    pub(super) runtime_sample_count: usize,
    pub(super) runtime_page_count: usize,
    pub(super) runtime_unique_nametable_count: usize,
    pub(super) runtime_temporal_screen_count: usize,
    pub(super) runtime_pages_with_visual_phase_change: usize,
    pub(super) runtime_visual_review_passed: bool,
    pub(super) initial_selector_runtime_bound_to_build: bool,
    pub(super) page_reload_runtime_bound_to_build: bool,
    pub(super) final_exit_runtime_bound_to_build: bool,
    pub(super) runtime_bound_to_build: bool,
}

#[derive(Debug, Serialize)]
pub(super) struct CumulativeDialogueLifetimeReport {
    pub(super) screen_role: &'static str,
    pub(super) chapter_index: u8,
    pub(super) screen_evidence_manifest_sha1: String,
    pub(super) installed_record_count: usize,
    pub(super) installed_translated_line_count: usize,
    pub(super) source_storage_byte_count: usize,
    pub(super) planned_storage_byte_count: usize,
    pub(super) remaining_storage_byte_count: usize,
    pub(super) unique_glyph_count: usize,
    pub(super) glyph_assignment_sha1: String,
    pub(super) preserved_screen_active_code_count: usize,
    pub(super) preserved_source_active_code_count: usize,
    pub(super) preserved_active_code_count: usize,
    pub(super) temporal_sample_count: usize,
    pub(super) unique_nametable_count: usize,
    pub(super) font_physical_page: u8,
    pub(super) font_mapper_register: u8,
    pub(super) font_page_sha1: String,
    pub(super) font_page_pack_sha1: String,
    pub(super) runtime_evidence_manifest_sha1: Option<String>,
    pub(super) runtime_sample_count: usize,
    pub(super) runtime_unique_image_count: usize,
    pub(super) runtime_bound_to_dialogue_stage_output: bool,
}

#[derive(Debug, Serialize)]
pub(super) struct SelectorFallbackGraphReport {
    pub(super) schema: u8,
    pub(super) node_count: usize,
    pub(super) route_count: usize,
    pub(super) multi_entry_target_count: usize,
    pub(super) direct_entry_candidate_count: usize,
    pub(super) conditional_entry_count: usize,
    pub(super) terminal_fallback_count: usize,
    pub(super) generated_selector_structure_bound: bool,
    pub(super) active_fixed_direct_entry_candidates_partitioned: bool,
    pub(super) nodes: Vec<SelectorFallbackNodeReport>,
    pub(super) routes: Vec<SelectorFallbackRouteReport>,
}

#[derive(Debug, Serialize)]
pub(super) struct SelectorFallbackNodeReport {
    pub(super) role: &'static str,
    pub(super) cpu_range_hex: String,
    pub(super) mapper_registers_hex: Vec<String>,
    pub(super) admitted_chapter_indices: Vec<u8>,
}

#[derive(Debug, Serialize)]
pub(super) struct SelectorFallbackRouteReport {
    pub(super) source_role: &'static str,
    pub(super) source_cpu_address_hex: String,
    pub(super) transfer_kind: &'static str,
    pub(super) target_role: &'static str,
    pub(super) target_cpu_address_hex: String,
}
