# 장 전환 화면 계약

기준일: 2026-08-12

장 종료부터 다음 장 도입까지를 한 화면으로 일반화하지 않는다. 실제 이동·턴 종료·전투와 `しろ` 선택을 거쳐 연속 관측한 11장→12장 기본 흐름은 다음 여섯 수명이다. 이동력·HP 진행 가속은 선언했지만 좌표, 주 상태, 승리 단계는 직접 쓰지 않았다. 저장 완료 질문의 “아니오”는 기본 흐름으로 복귀하지 않고 종료 안내와 사운드 테스트라는 두 별도 수명을 연다.

| 순서 | 화면 역할 | 상태 / CHR `$59..$5C` | 입력과 효과 | 번역 경계 |
| ---: | --- | --- | --- | --- |
| 1 | `chapter_clear_epilogue_dialogue` | `$24=0C`, `$84=3C`, `$053E=02`, `$77F7=0E` / `11 11 00 18` | 본문은 자동 출력, 완료 뒤 `A`는 다음 페이지 또는 배너로 진행 | 일본어 대사만 번역 |
| 2 | `next_story_banner` | `$24=0D`, `$84=03` / `1B 1B 00 18` | 1,200프레임 무입력 유지, `A`는 저장 제안으로 이동 | 원본 영어 `NEXT STORY` 보존 |
| 3 | `chapter_save_offer` | `$24=0D`, `$84=07` / `1B 1B 00 18` | 위·아래는 `$7FF4`의 선택만 변경. “예”는 저장을 기록하고, “아니오”는 결과 `02`를 `$05EB`에 넣어 저장 완료 질문을 건너뜀 | 일본어 UI만 번역 |
| 4 | `chapter_save_complete_continue_prompt` | `$24=0E`, `$84=04`, `$77F7=11` / `1C 1C 00 18` | 위·아래는 `$7FF5`의 선택만 변경. “예”는 자동 검은 전환, “아니오”는 종료 안내로 이동 | 일본어 대사·UI만 번역 |
| 5 | `chapter_transition_blackout` | 저장 경로 `$24=09` / `1A 1A 18 18`; 비저장 경로 `$24←01` / `1B 1B 18 18` | 두 경로 모두 무입력 자동 전이 | 텍스트 없음 |
| 6 | `chapter_intro_title_dialogue_composite` | `$24=0B`, `$84=00`, `$77F7=0E` / `0F 0F 00 18` | 본문은 자동 출력, 완료 뒤 `A`는 대사를 진행 | 일본어 제목·대사만 번역하고 장 번호는 보존 |

| 분기 단계 | 화면 역할 | 상태 / CHR `$59..$5C` | 입력과 효과 | 번역 경계 |
| ---: | --- | --- | --- | --- |
| 5-N | `chapter_save_complete_power_off_notice` | `$24=0E`, `$84=04`, `$05EE=0A` / `1C 1C 00 18` | 일본어 종료 안내가 자동으로 그려진 뒤 대기. 숨은 6입력만 사운드 테스트를 엶 | 일본어 안내만 번역 |
| 6-N | `sound_test` | `$24=0E`, `$84=04`, `$05EE=0C` / 그림자 `1C 1C 00 18` | 숨은 입력 뒤 자동 진입; 위·아래와 `A/B`는 번호·순간 음원 이벤트를 바꾸고, `START`는 공용 전투 화면 반복, `SELECT`는 자동 엔딩으로 이탈 | 원본 영어와 숫자 전부 보존 |

11장 종료 대사는 완료 페이지 네 개였고 초상화 표본은 차례로 표시·표시·비표시·표시였다. 승리 단계 `$053E=02`는 페이지마다 유지되므로 페이지 진행 입력은 대사 완료 상태 `$77F7=0E`에 결속한다. 저장 제안, 저장 완료 질문, 종료 안내는 창 크기·초상화·본문·입력 부작용이 다르므로 같은 `chapter_save` 역할로 합치지 않는다. 저장 제안의 “아니오”는 저장 완료 화면을 건너뛰되 별도의 검은 자동 전환을 거치므로, 화면 역할은 `chapter_transition_blackout`을 재사용하고 상태·CHR 변형은 분리한다. 저장 완료 질문의 “아니오”는 반대로 바깥 상태를 유지한 채 종료 안내에 정착한다. 다음 장 도입도 장 제목 바와 대사창이 동시에 보이는 합성 화면으로 관리한다.

7장 성 좌표 `(27,10)`의 종료 대사는 별도 선택자 `C0:18`이며 15개 완료 페이지를 갖는다. 본문은 자동 출력되고 `$77F7=0E`에서만 `A`를 수락하며, 중간 입력은 다음 페이지로, 마지막 입력은 `$24/$84=0D/03`의 원본 영어 `NEXT STORY`로 이동한다. 각 페이지는 0·7·19·43·82·171프레임의 불규칙 표본으로 점멸을 포함했다. 레코드 전체 상주 수요 `275/210`은 불가능하지만 최대 단일 페이지 `135/210`은 수용 가능하므로, 이 화면은 완료 페이지 경계의 글꼴 재적재를 요구한다. 11장의 네 페이지 `C0:1C`와 같은 수명으로 합치지 않는다.

화면별 조사는 먼저 화면 역할과 진입·이탈, 번역 대상과 보호할 원문, 배경·초상화·창·스프라이트의 합성, 점멸·자동 출력, 입력별 부작용을 정한 뒤 국소 주소와 실행 경로로 각 빈칸을 닫는다. 이 순서를 따르면 같은 CHR 쌍을 쓴 저장 완료 질문·종료 안내·사운드 테스트를 한 화면으로 잘못 합치지 않는다.

## 정적 결속

- 장 인트로 표의 고유 `E5` 문맥은 25개다. 5바이트 페이로드는 차례로 `$71`, `$70`, `$05CF`, `$05D0`, `$781D`에 들어가며 마지막 값은 `00..18`로 연속된다.
- 장 제목 소비자 `0B:$88C4`는 `$781D`를 읽어 고정 뱅크 `$EE08`의 포인터 표를 고른다. 표는 24개가 아니라 25개이며, 마지막 포인터 `$EFA8`의 문자열은 다음 장소 이름 표가 시작되는 파일 `3EFC7` 직전에서 끝난다.
- 장 제목 25개에는 원본 장 번호 숫자 41바이트가 있다. 숫자는 보호하고 일본어 제목 부분만 번역한다.
- `NEXT STORY`는 고정 라벨 인덱스 `3E`, `$91FB`이고 `0B:$886A`가 합성한다. 번역하지 않는다.
- 첫 저장 질문 `セーブしますか?`는 고정 라벨 인덱스 `32`, `$91AA`이고 `0B:$8AE6`가 합성한다.
- 바깥 `$24=0D`의 `06:$B5AC`가 `$84` 하위 상태를 분배한다. 저장 질문의 상태 `07/08/09`는 각각 `06:$B6F3/$B726/$B737`이다. 상태 `07`은 `$05EB==02`를 “아니오”로 판정해 저장 화면 `$24=0E` 경로를 타지 않고 상태 `08`로 간다. 상태 `09`는 `$84`를 `00`으로 지운 뒤 `06:$B76E`에서 `$24=01`을 쓴다.
- 공용 메뉴의 `0B:$9333`은 `$05CE-1`로 활성 선택 슬롯을 고른다. 관측한 깊이 `$05CE=02`에서는 부모 슬롯 `$7FF3`이 아니라 `$7FF4`가 현재 선택이다. `Down`은 `0B:$936C`에서 `01→02`, 확정은 `0B:$9391`에서 결과 `02`를 `$05EB`에 썼다. 이 실행 코드 구간은 모두 typed RP2A03 디코드를 통과해야 보고서에 들어간다.
- 바깥 `$24=0E`는 `06:$B771`에서 `$84`를 분배하고, 관측한 주 상태 `04`는 `06:$B7CB`에서 뱅크 `0B:$995F`의 `$05EE` 하위 상태표를 호출한다. 저장 완료 질문의 선택 상태 `07`은 `0B:$9B35`다. 깊이 `$05CE=03`의 활성 슬롯은 `$7FF5`이며 “아니오” 결과 `02`는 상태 `08/09/0A`의 종료 안내 경로로 간다. 기본 “예” 결과는 `$05EE=FF` 뒤 증가로 상태 `00`에 복귀해 다음 장 계속 경로를 연다.
- 종료 안내의 상태 `0A`는 `0B:$9BA0`에서 현재 입력을 `08 04 02 01 08 80`과 순서대로 비교하고 진행도를 `$775B`에 둔다. 이는 `위·아래·왼쪽·오른쪽·위·A`이며, 완성하면 상태 `0B`의 `0B:$9BCF`가 화면을 초기화하고 상태 `0C` 사운드 테스트를 연다. 이 코드와 데이터 구간도 typed RP2A03 코드·자료로 분리해 검사한다.
- 사운드 테스트 상태 `0C`의 `0B:$9C17`은 입력 `$18`을 직접 분기한다. 위·아래는 번호 `$775C`를 바꾸고, `A`는 선택 번호를 `$06F0..$06F7`의 단일 비트로 변환하며, `B`는 `$06F0=01`을 쓴다. 화면의 표시 숫자는 `0B:$9C75`가 다시 합성한다.
- `START`는 상태 `0D`, 뱅크 `07` 처리기 3의 `$AA2B`로 들어가 `$7730`의 6개 단계 포인터를 반복한다. `SELECT`는 상태 `0E/0F`, 뱅크 `04` 처리기 4의 `$9EC6`로 들어가 `$7731`의 30개 단계 포인터를 분배한다. 실행에서 `START`의 바깥 단계는 19프레임 안에 `05`에 도달한 뒤 공용 전투 엔진의 여러 가시 수명을 반복했고, `SELECT`는 무입력으로 `01→08→10→1D`를 거쳤다. 따라서 6·30이라는 원천 단계 수는 화면 수가 아니다.
- 바깥 `$24=0D`·주 상태 `$84=00`의 `06:$B5C8`은 `06:$B67B`를 호출한다. 이 생산자는 포인터 하위 바이트 `90`과 상위 바이트 `6A`로 만든 `$6A90`부터 27바이트 레코드를 ID `00`까지 훑고, 행동 바이트 오프셋 `12`가 `FF`인 비활성·격파 레코드마다 현재 장 `$7674`를 오프셋 `0E`에 기록한다. 후일담의 표 `41` 분기가 같은 `12`를 판정하고 같은 `0E`를 장소 이름으로 소비하므로 두 필드는 장별 이력으로 연결된다. 단, 원천만으로 `FF`의 모든 원인을 인물 사망으로 한정하지 않는다.

선택된 “아니오” 화면은 입력 없이 0·7·19·43·82·171·308·565프레임에서 모두 같은 PNG SHA-256 `a05ea43bc1844701650cfee6d441862d16d60fc1f7f0035704ce15dbbcfb352c`였다. 이 표본에서는 선택 커서나 배경이 점멸하지 않았지만, 다른 장의 retained-map 변형까지 같다는 뜻은 아니다.

저장 완료 질문에서도 `$7FF5:01→02` 뒤 선택 화면은 같은 불규칙 여덟 표본에서 SHA-256 `c160f6fe5645e90e0f003dd1ccf64aef60b5a516eb2e09232e3717bb6f9f0496`로 같았다. 확정 뒤 안내는 1·11·30·67프레임에 자동으로 그려졌고, 130·259·516·900프레임은 모두 SHA-256 `c7e16901de2ed4e73c3cc6534e8a478e07ae61630ab7f7b43120f352f7c03ff1`로 정착했다. 숨은 입력은 각 단계에서 `$775B=00→05`를 확인하고 마지막 `A`가 `$05EE:0A→0B`를 만든 뒤에만 실행했다. 추가 입력 없이 상태 `0C`의 사운드 테스트가 나타났으며, 화면의 영어와 숫자는 번역하지 않는다.

사운드 테스트 기준 상태에서 위는 `$775C:00→01`, 아래는 `00→50`으로 바뀌었다. `A`는 선택 번호 1을 `$06F0=02`로, `B`는 `$06F0=01`로 기록했다. 두 음원 이벤트는 소비자가 프레임 안에서 다시 0으로 지우므로 일반 정지 화면의 사후 메모리만으로는 보이지 않고, 원천 쓰기 경계와 값 필터로 확인했다. 화면의 영어 라벨과 숫자는 모든 조작에서 보존한다.

`START` 뒤에는 입력을 더 주지 않았다. 불규칙 표본 0·1·7·19·43·82·171·308·565·711·934·1273·1691·2200·3000프레임에서 검은 전환, 한쪽·양쪽 유닛 정보, 전투 예측, 공격, 피해, 필살, 격파와 EXP, 다음 유닛 쌍의 공격까지 나타났다. 바깥 `$7730`은 19프레임부터 계속 `05`였으므로 이 표면들은 테스트 전용 6화면이 아니라 공용 `battle_animation` 안의 가시 수명이다. 일본어 유닛명·무기·행동·결과 문구만 번역하고 `LV`, `HIT`, `EXP`, HP 막대와 숫자는 보존한다.

`SELECT` 뒤에도 입력을 더 주지 않았다. `$7731=01`은 원본 영어 엔딩 문장과 `CAST`·로마자 이름을 자동 스크롤한 뒤 일본어 장 제목·턴 기록을 같은 스크롤에 합성했다. 상태 `08`은 원본 영어 스태프, 상태 `10`은 초상화와 일본어 인물 후일담의 자동 반복, 상태 `1D`는 원본 서명·`©1990 Nintendo`의 정적 종단이었다. 영어·로마자·숫자·서명·저작권은 보존하고 일본어 장 기록과 인물 후일담만 번역한다. 상태 `1D`는 no-op 처리기 `$C73D`를 쓰고 `$7732=00`이며, 21,000·24,000·28,000·33,000프레임의 스크린샷·네임테이블·OAM·팔레트가 모두 같았다. 이전의 검은 표본은 종단 진입 전 전환이지 서명 점멸이 아니었다.

`analyze-chapter-transitions`는 이 소비 코드·포인터·라벨과 25개 장 문맥을 지원 일본판에서 다시 검사하고 `out/chapter-transitions.json`을 만든다. 장 종료 대사와 저장 완료 대사의 원문은 보고서에 넣지 않는다.

## 전투와 엔딩의 상위 원천 결속

사운드 테스트의 `START`는 뱅크 `05:$8161`의 공용 전투 엔진을 부르며 `$047C`의 32개 내부 단계를 자동 진행한다. 보이는 유닛명·적 이름·병종·아이템은 기존 공용 네 표를 사용하고, 전투 메시지는 뱅크 `04:$845B`의 65포인터 별도 표를 `$8000` 로더로 읽는다. 별도 상태기 `$7937`은 4바이트 헤더 뒤 `EC`의 선택자 한 바이트를 확장하고 `EF`에서 끝낸다. 고유 포인터 대상 28개는 겹치지 않는 1,152바이트이고, 포인터 없는 16바이트 레코드 하나는 번역 대상으로 추정하지 않고 보존한다. 따라서 디버그 화면만 따로 번역하지 않고 실제 전투 소비자 하나를 기준으로 관리한다.

엔딩 상태 `01`의 `$A826..$ACC8` 스트림은 113레코드다. 앞 43개는 보존할 원본 영어·캐스트·레이아웃이고, 이어지는 25개 장 행은 장 제목 표와 의미상 같은 일본어를 다른 레이아웃으로 중복 저장하며 `ED 00..18`로 장별 턴 수를 삽입한다. 합계 행은 `ED 19`를 쓴다. 승인된 장 제목 번역 하나가 장 제목 표와 엔딩 행 두 물리 인코딩을 함께 생성해야 하며, 장 번호 41바이트와 실행 중 생성되는 턴 숫자는 보존한다. 인물 후일담은 상태 `0E`가 후보 커서 `$773B=35`를 만들고 상태 `0F`가 후보 ID `35..01`을 역순으로 훑는다. `$6A90`의 54개 27바이트 레코드에서 같은 ID를 찾지 못하면 건너뛰고, 일치 레코드의 행동 바이트 `12`가 비활성·격파 값 `FF`이면 표 `41`과 레코드 오프셋 `0E`의 장별 장소 이름을, 아니면 표 `40`을 고른다. 상태 `10`은 공용 대사 완료를 기다린 뒤 추가 입력 없이 진행한다. 렌더링 합집합은 두 분기 모두 닫혔지만, 합성 분기 도달만으로 `FF`의 모든 자연 진행 원인을 사망으로 한정하지 않는다.

후일담 변형 합집합은 사운드 테스트의 결속된 `SELECT` 효과로 자동 엔딩에 진입한 뒤, 첫 선택자 명령 `04:$A165`에 중단점을 둔다. backend는 그 주소의 `DEC $773B`를 완료하고 PC `$A168`에서 멈추므로, 커서 `34`를 `+1`한 첫 후보 ID `35`의 이름 합성·분류 직전 instruction-boundary 상태를 공통 기준으로 삼는다. 자연 기준은 메모리를 바꾸지 않고 부재·비활성·활성 분기를 함께 관측한다. 전 인물 직접 분기는 유효 레코드 사본 53개의 ID만 `01..35`, 행동을 `FF`가 아닌 값으로 만들고 54번째 레코드에 ID `00` 종단을 둔다. 라우팅 분기는 같은 구성에서 행동을 `FF`, 장소를 유효 인덱스로 둔다. 허용 쓰기 범위는 `$6A90`부터 54×27바이트뿐이며 `$7731/$773B/$77F1/$77F4`의 단계·후보·엔트리·표 선택자는 쓰지 않는다. 각 보이는 엔트리는 시작점으로부터 7·19·43·82·171프레임의 불규칙 오프셋에서 화면·네임테이블·OAM·팔레트·CPU/매퍼 상태를 모아 점멸과 점진 표시를 놓치지 않는다. 이 격리 실험은 렌더링·제어 흐름 합집합 근거일 뿐 자연 진행, 난이도, 인물의 정확한 운명이나 완료 경로 근거가 아니다.

세 실행에서 자연 7개, 직접 53개, 라우팅 52개의 보이는 엔트리를 얻었고, 560개 표본은 모두 완전했다. 합집합은 왼쪽 `04/04`, `07/07`, `0A/0A`, `0B/0B`, `0F/0F`, `10/10`, `11/11`, `13/13`, `17/17`, `1A/1A`, `1C/1C`, `1D/1D`, `1F/1F`와 오른쪽 `00/00`이다. 라우팅 `41:01`은 상태 `10`의 빈 화면에서 600프레임 더 머물러 보이는 엔트리로 세지 않는다. 직접 경로의 `$77F1` 쓰기 이벤트는 후보 `01..35`와 확장 `36..39`, `3B..3D`, `3F..41`을 기록했으며 `3A/3E`는 관측되지 않았다. 구조 보고서는 원문 대사와 증거 경로를 내보내지 않는다.

번역 표면 결속은 원문을 내보내지 않고 실제 리터럴 위치만 합쳐 일본어·보존 원문·레이아웃 코드 집합을 계산한다. 이름·병종·아이템·지형의 다섯 전투 고정 표 107코드는 `100/6/1`이고, 별도로 22개 짧은 메시지 템플릿과 인라인 예측 라벨의 위치·소비자를 결속한다. 전투 대사 834바이트는 `620/167/47`, 엔딩 장 기록 439바이트는 `349/47/43`, 인물 후일담 1,649바이트는 `1,529/9/111`로 나뉘며 미해결 코드는 없다. CHR과 사용 문맥에서 `8E = 、`, `8F = 。`, `AB/AC = 「/」`는 일본어 원문, `9D = !`는 보존 원문으로 확정했다. 선택자 `41`의 52개 라우팅 스크립트는 표시 리터럴 없이 모두 포함된 선택자 `40` 표로 전이하므로 후일담 집합에서 빠지는 외부 표가 없다.

## 후반 장 도달 표본

정규 파일 1은 1-based 장 번호를 `$6519`에 둔다. 로더는 `$6000..$6542`의 모든 바이트를 16비트 가산한 값이 little-endian `$6542/$6543`과 같을 때만 파일을 받아들인다. 체크섬 계산기는 `0B:$9D52`, 파일 1 기록 분기는 `0B:$9AD0`, 검증 분기는 `0B:$9FA8`이다.

격리된 원본 실행에서 1장 완료 파일의 장 번호와 체크섬만 함께 바꿔 12장 도입을 열었다. 이는 자연스럽게 11장을 완료한 결과가 아니라 화면 도달 개입이다.

| 표본 | 진입 | `$7674` / `$781D` | CHR `$59..$5C` | 확인 범위 |
| --- | --- | --- | --- | --- |
| 2장 | 1장 완료 및 정규 파일 콜드 로드 | `02 / 01` | `13 13 00 18` | 장 제목·맵·초상화·대사 합성 화면 |
| 11장 | 장 번호·체크섬을 바꾼 정규 파일 1 | `0B / 0A` | `1A 1A 00 18` | 같은 합성 역할, 초상화 없는 표본 페이지, 점멸 완료 표식의 표시·비표시 위상 |
| 12장 | 11장 성 명령부터 이어진 연속 경로; 콜드 로드도 별도 확인 | `0C / 0B` | `0F 0F 00 18` | 같은 합성 역할, 다른 왼쪽 CHR 쌍, 점멸 완료 표식의 표시·비표시 위상 |

11장과 12장 표본은 장 번호 숫자를 원본으로 보존하면서 일본어 장 제목과 대사가 함께 살아 있는 후반 변형을 확인한다. 세 도입 표본의 왼쪽 쌍은 모두 달랐고, 캡처한 11장 완료 페이지에는 2장·12장과 달리 초상화가 없었다. 25개 장이 이 셋 중 하나만 쓰거나 장마다 초상화 유무가 고정된다는 뜻은 아니다.

## 다음 관문

11장 성 명령부터 12장 도입까지의 연속 도달, 7장 `C0:18`의 15페이지 수명, 저장 제안과 저장 완료의 두 “아니오”, 사운드 테스트의 여섯 버튼, 전투 극성, 자동 엔딩·정적 종단과 인물 후일담 변형 합집합까지 닫혔다. `C0:18`은 완료 페이지 경계마다 실제 한글 글꼴 작업 집합을 재적재하고, 초기 선택기부터 15페이지·점멸 위상·최종 이탈까지 현재 출력에 결속했다. 소지품 화면 여섯 개는 사용 결과 `43/210`을 포함해 계수를 마쳤고, 시작 메뉴 `27/210`과 설정 화면 `90/210`도 현재 누적 빌드에 결속했다. 다음 구조 관문은 남은 미계수 화면 10개를 같은 기준으로 비교하는 것이다. 다른 장의 종료 대사·초상화·CHR·제목 바 변형과 `FF` 분기의 자연 진행 의미는 별도 실행·의미 관문으로 유지한다.
