# 장 전환 화면 계약

기준일: 2026-08-07

장 종료부터 다음 장 도입까지를 한 화면으로 일반화하지 않는다. 현재 실행으로 확인한 1장→2장 흐름은 다음 다섯 수명이다.

| 순서 | 화면 역할 | 집중할 표면 | 입력과 효과 | 번역 경계 |
| ---: | --- | --- | --- | --- |
| 1 | `chapter_clear_epilogue_dialogue` | 기존 맵, 유닛, 초상화, 대사창, 완료 표식 | 본문은 자동 출력, 완료 뒤 `A`는 다음 페이지 또는 배너로 진행 | 일본어 대사만 번역 |
| 2 | `next_story_banner` | 기존 맵 위 중앙 창 | 1,200프레임 무입력 유지, `A`는 저장 제안으로 이동 | 원본 영어 `NEXT STORY` 보존 |
| 3 | `chapter_save_offer` | 작은 저장 질문, 예/아니오 창, 커서 | 위·아래는 선택만 변경, 확인된 기본 “예”의 `A`는 저장을 기록 | 일본어 UI만 번역 |
| 4 | `chapter_save_complete_continue_prompt` | 초상화, 큰 완료 대사창, 예/아니오 창, 커서 | 확인된 기본 “예”의 `A`는 다음 장 도입으로 이동 | 일본어 대사·UI만 번역 |
| 5 | `chapter_intro_title_dialogue_composite` | 새 맵, 장 제목 바, 초상화, 대사창, 완료 표식 | 본문은 자동 출력, 완료 뒤 `A`는 대사를 진행 | 일본어 제목·대사만 번역하고 장 번호는 보존 |

저장 제안과 저장 완료는 창 크기, 초상화, 본문, 입력 부작용이 다르므로 같은 `chapter_save` 역할로 합치지 않는다. 다음 장 도입도 장 제목 바와 대사창이 동시에 보이는 합성 화면으로 관리한다.

## 정적 결속

- 장 인트로 표의 고유 `E5` 문맥은 25개다. 5바이트 페이로드는 차례로 `$71`, `$70`, `$05CF`, `$05D0`, `$781D`에 들어가며 마지막 값은 `00..18`로 연속된다.
- 장 제목 소비자 `0B:$88C4`는 `$781D`를 읽어 고정 뱅크 `$EE08`의 포인터 표를 고른다. 표는 24개가 아니라 25개이며, 마지막 포인터 `$EFA8`의 문자열은 다음 장소 이름 표가 시작되는 파일 `3EFC7` 직전에서 끝난다.
- 장 제목 25개에는 원본 장 번호 숫자 41바이트가 있다. 숫자는 보호하고 일본어 제목 부분만 번역한다.
- `NEXT STORY`는 고정 라벨 인덱스 `3E`, `$91FB`이고 `0B:$886A`가 합성한다. 번역하지 않는다.
- 첫 저장 질문 `セーブしますか?`는 고정 라벨 인덱스 `32`, `$91AA`이고 `0B:$8AE6`가 합성한다.

`analyze-chapter-transitions`는 이 소비 코드·포인터·라벨과 25개 장 문맥을 지원 일본판에서 다시 검사하고 `out/chapter-transitions.json`을 만든다. 장 종료 대사와 저장 완료 대사의 원문은 보고서에 넣지 않는다.

## 다음 관문

현재 실행 표본은 1장→2장과 2장 콜드 로드뿐이다. 저장 제안·저장 완료 각각의 정확한 CHR 쌍, 커서·완료 표식의 점멸 위상, “아니오” 분기, 3장 이후의 지도·초상화·장 제목 변형은 미확정이다. 다음 실행은 더 뒤의 장 전환을 재현해 같은 다섯 역할이 유지되는지 확인하되, 2장 표본을 전체 25장에 자동 일반화하지 않는다.
